# DESIGN_BACKLOG — Vittro

Deuda visual, migraciones y tareas pendientes para que el código cumpla las reglas
de `DESIGN.md`. Este archivo es el checklist operativo; la regla vive en
`DESIGN.md`, acá vive solo el trabajo para llegar a ella.

- **Estados:** Pendiente · En progreso · Bloqueado · Resuelto · Descartado
- **Prioridades:** P1 = visible / impacto directo en usuarios · P2 = consistencia del sistema · P3 = higiene
- Origen: auditoría de exploración `document` (2026-08-22), sobre la base de las
  auditorías previas de `CRITERIOS_DISEÑO.md`.
- Regla de proceso: ningún ítem se resuelve "de pasada" dentro de otro build sin
  mencionarlo; al resolver uno, actualizar su estado acá.

## P1 — Visible / impacto directo

| ID | Estado | Hallazgo | Regla asociada | Acción pendiente | Afecta | Riesgo / dependencias |
|---|---|---|---|---|---|---|
| D01 | Pendiente | Auth usa un micro-sistema propio: ~20 hex slate, navy pintado a mano (`#2a3a60` ≈ `primary/90`), `rounded-[10px]` en botones/selects | Auth pertenece al núcleo visual (DESIGN.md Overview) | Build de migración de Auth a tokens del núcleo (color, radios, foco, tipografía) | `Login.tsx`, `VerifyEmail.tsx`, `AuthCallback.tsx` | Primera pantalla de todo usuario nuevo. Dep: D07 (token `--radius-tile` creado antes, para que Auth no herede 10px por accidente) |
| D02 | **Resuelto** (C2, 2026-08-22) | Controles editables custom sin garantía de 16px en mobile | Regla de los 16px en mobile (DESIGN.md Typography) | Auditoría de C2 confirmó `textarea.tsx`/`currency-input.tsx`/`phone-input.tsx`/`Select` ya cumplían por herencia de `Input`. Los incumplimientos reales — 6 date/time nativos (`BloqueosSection`, `ScheduleEditor`), 2 `CurrencyInput` comprimidos (`BonoFijoConfig`, `ProductoPickerDialog`) y `CommandInput` — corregidos en Build A | `ui/command.tsx`, `productos/ProductoPickerDialog.tsx` + los archivos migrados a `DatePicker`/`TimePicker` | Los `type="date"`/`type="time"` nativos fuera del alcance de C2 no reabren este ítem — su migración vive en D34 |

## P2 — Consistencia del sistema

| ID | Estado | Hallazgo | Regla asociada | Acción pendiente | Afecta | Riesgo / dependencias |
|---|---|---|---|---|---|---|
| D03 | Pendiente | Botón "Activar" con `bg-green-50 text-green-600 hover:bg-green-100 dark:…` copiado literal en 6 archivos | Regla del estado con token | Migrar a `--status-success-bg/-foreground`; de paso quitar las variantes `dark:` (D24) | `ServicesConfig`, `LinesConfig`, `ExtrasConfig`, `DiscountsConfig`, `ProductosGlobalConfig`, `ProductoListItem` | Verificado: mismo verde percibido que el token — migración segura |
| D04 | Pendiente | Import de clientes usa `emerald`/`amber` directos (tercer verde distinto de la app) | Regla del estado con token | Migrar a `--status-success`/`--status-warning` | `ImportSummaryStep`, `ImportPreviewStep`, `MergeDuplicatesDialog` | — |
| D05 | Pendiente | `useBarberColors` usa una paleta hardcodeada (2 tokens + 8 hex Tailwind) para un **acento auxiliar de UI** de Agenda — no representa un dato de barbero (`barberos` no tiene color persistido) ni es el sistema principal de color de turnos (ese rol lo cumple `lineas.color`, ver D25). Auditoría de C1 (2026-08-22) revirtió una consolidación prematura en `--barber-1…10` por confundir este acento con un sistema de identidad de marca — ver `DESIGN.md` § Colors, "Color de turnos en Agenda" | Sin regla asociada todavía — pendiente de decisión | Decidir el tratamiento de este acento (token, paleta reducida, o dejarlo como hex) **junto con el sistema visual de Agenda**, no de forma aislada | `agenda/hooks/useBarberColors.ts` (consumido en `AgendaDayView.tsx`, `AgendaMultiDayView.tsx`/`AgendaMultiDayColumn.tsx`, `DailyTurnosViewer.tsx`) | No repetir el error de tratarlo como paleta de identidad — es encabezado de columna + badge "eligió barbero" + fallback puntual, nada más |
| D06 | Pendiente | Sidebar con hex propios: `#F4F5F7` (≈`--muted`) ×2 hover, `#EEEFF2` (≈`--border`) ×2, más hairline `#EEEFF2` en el menú del selector de sucursal | UI = tokens | Migrar a `--muted`/`--border`, o tokenizar la variante si la diferencia sutil resulta deliberada al verificar en build | `AppSidebar.tsx:141,151,245,378`, `SucursalSelector.tsx` | Baja visual, alta conceptual: el sidebar quedaría desincronizado ante un cambio de paleta |
| D07 | Pendiente | Token `--radius-tile: 10px` declarado en DESIGN.md pero inexistente en CSS; 13 usos literales de `rounded-[10px]` | Tres radios con rol (DESIGN.md Shapes) | Crear el token + clase; migrar los tiles de marca (`AppSidebar`, `PageHeader`, `SucursalSelector`, `SubscriptionGate`, `ResumenMetodosCard`, `ResumenMetricaCard`). Los usos de Auth desaparecen con D01 | 6 componentes + Auth | — |
| D08 | **Resuelto** (C3, 2026-08-22) | Tabs pill/underline usados para filtros o subvistas contextuales — violaban la regla jerárquica de navegación | Tabs = secciones hermanas / SegmentedControl = filtro, modo o subvista contextual | Migrados a `SegmentedControl`: `RecurrentesPanel` (Activas/Pausadas), `HorariosAtencionCard` (Sucursal/Barberos), `MarcasManagerDialog` (Activas/Inactivas, desde sus 2 accesos). **No migran** — quedaron caracterizados como excepciones aprobadas o fuera de alcance: `TareasPanel` (Tareas/Peticiones son secciones hermanas, no filtro — decisión de producto revirtió la lectura original de C3), `NotificationsBell` y `ProductoDialog`/`BackfillWizard` (excepciones ya documentadas en DESIGN.md), `AgendaPanel` (no es Tabs/SegmentedControl, es `ToggleGroup`). `StaffConfig` no se tocó — es código muerto sin ruta UI alcanzable, pertenece a D22, nunca fue parte de este ítem | 3 archivos migrados | El plan original de C3 incluía `TareasPanel` y `StaffConfig` en el alcance; el build los excluyó por decisión de producto (Tareas) y por pertenecer a otro capítulo (StaffConfig) |
| D09 | **Resuelto** (C3, 2026-08-22) | 4 estilos ad-hoc del pill de `TabsList` re-estilizado por caller | Un solo aspecto por control | La deuda existía y se resolvió en los consumidores correspondientes a C3: `RecurrentesPanel.tsx` (Tareas → Tareas → Recurrencias, "Activas/Pausadas") y `MarcasManagerDialog.tsx` (Mi Negocio → General → Productos → Marcas **y** Mi Negocio → [Sucursal] → Productos → Marcas, "Activas/Inactivas") reemplazaron su pill ad-hoc por `SegmentedControl` canónico. Los estilos que quedan (`ProductoDialog`, `BackfillWizard`, `NotificationsBell`) pertenecen a excepciones aprobadas del canon (documentadas en DESIGN.md); el de `StaffConfig` es código muerto pendiente de C12 — ninguno de los dos mantiene este ítem abierto. No se incluyen acá divergencias tipográficas de otros capítulos | `RecurrentesPanel.tsx`, `MarcasManagerDialog.tsx` | — |
| D10 | Pendiente | Doble sistema de toasts: `sonner` (83 consumidores) + shadcn-toast (2), ambos Toasters montados en `App.tsx` | sonner único sistema (DESIGN.md Feedback) | Migrar `NotificationsConfig` y `PaymentRegistration` a sonner; desmontar `<Toaster/>` viejo; eliminar `ui/toast.tsx`/`ui/toaster.tsx`/`use-toast.ts` cuando queden sin consumidores | `NotificationsConfig.tsx`, `PaymentRegistration.tsx`, `App.tsx`, `ui/toast*` | Evitar que componentes futuros reintroduzcan el sistema viejo |
| D11 | Pendiente | ~30 usos de `focus:ring` (anillo visible también con mouse) vs el canon `focus-visible` | focus-visible único patrón | Auditar y normalizar los ~30 usos a `focus-visible:` | transversal (varios de shadcn base) | — |
| D12 | Pendiente | Descripciones de campo en `text-[11px]` — texto de lectura por debajo del piso de 12px | Regla de densidad legible | Subir a `text-xs` las descripciones; revisar el resto de `text-[11px]` de la app interna distinguiendo descripción (sube) de metadata (queda) | `AgendaConfigSection` ×3, `PaymentRegistration` ×5, + barrido de `text-[11px]` | Los 7–9px de `Homepage.tsx` son mockup en miniatura, NO deuda |
| D13 | Pendiente | 203 controles interactivos ≤32px; kebab canónico de 28px | Targets táctiles híbridos (DESIGN.md Do's) | Auditar por categoría: acciones primarias/frecuentes → target cómodo; controles secundarios de fila densa → ampliar área táctil real (hit-area) sin inflar lo visible | app interna, foco en tablet/mobile | No sacrificar densidad; priorizar área táctil invisible |
| D14 | Pendiente | `useIsMobile` corta en 768px mientras el CSS de layout corta en `sm:` 640px — banda 640–768 con respuestas contradictorias | Breakpoints: CSS y JS misma condición (DESIGN.md Layout) | Auditar los 7 consumidores de `useIsMobile`: los que deciden layout se alinean con la media query correspondiente; los que deciden capacidad táctil migran a `pointer`/`hover` | 7 archivos consumidores + `hooks/use-mobile.tsx` | — |
| D15 | Pendiente | Empty state rico definido inline en `TareasPanel` y no exportado; el resto de la app usa líneas sueltas sin regla | Empty states de dos niveles (DESIGN.md Components) | Extraer componente compartido (ícono + label + hint + acción opcional); adoptarlo en vacíos de sección/primera vez; dejar la línea simple en vacíos de filtro | `TareasPanel.tsx` (origen) → `ui/` | — |
| D16 | **Resuelto** (documental, C2, 2026-08-22) | "Nueva sucursal" usa `Dialog` centrado en vez del canon | DrawerForm canon de alta/edición | La auditoría de C2 confirmó en código (`MiNegocioPanel.tsx`) que el flujo ya usa `DrawerForm` + RHF/Zod + `isDirty` + footer canónico — deuda histórica desactualizada, sin código que migrar | `MiNegocioPanel.tsx` | Ninguno — no se tocó código |
| D32 | Pendiente | Fallback de color de turno inconsistente entre vistas cuando el servicio no tiene línea asignada: vista Día cae a `hsl(var(--border))`; Multi-Día/Cobrar cae primero al acento de barbero (D05) y recién después a `hsl(var(--primary))`. Mismo caso funcional ("servicio sin línea"), tres resultados visuales distintos según la vista | Sin regla asociada todavía — pendiente de decisión | Unificar el fallback en una sola regla, a resolver junto con D05 en el futuro capítulo de Agenda | `AgendaDayView.tsx`, `AgendaMultiDayColumn.tsx` | Descubierto en la auditoría de C1 (2026-08-22) al trazar el flujo real de color de Agenda |

## P3 — Higiene

| ID | Estado | Hallazgo | Regla asociada | Acción pendiente | Afecta | Riesgo / dependencias |
|---|---|---|---|---|---|---|
| D17 | Pendiente | Escala `--color-50…950` ("Vittro indigo") casi muerta: 1 solo uso (scrim mobile del sidebar), sin mapeo Tailwind | Navy = identidad vigente; indigo = legacy | Reemplazar el scrim por un token vivo y eliminar la escala completa | `index.css`, `AppSidebar.tsx:208` | Antes de borrar, verificar que no haya consumidores dinámicos ocultos |
| D18 | Pendiente | Tokens muertos: `--chart-1…5` (0 usos), `--sidebar` (duplica `--sidebar-background`), `--status-indigo-bg` (definido, sin mapear) | Sistema sin tokens fantasma | Eliminar los muertos; mapear `--status-indigo-bg` solo si aparece un consumidor real | `index.css`, `tailwind.config.ts` | — |
| D19 | Bloqueado | `--chart-orange`/`--chart-amber` marcados `/* PROVISIONAL */` desde julio | Paleta pendiente con el socio | Ratificar o redefinir al cerrarse la paleta de marca | `index.css` | **Bloqueado** por decisión de paleta con el socio |
| D20 | Pendiente | `rounded-xl` de contenedores no deriva de ningún token — el valor visual (12px) ya está cerrado en DESIGN.md, pero en código sigue siendo un literal de Tailwind sin rol semántico propio | Tres radios con rol — contenedores = 12px, decisión cerrada, no se reabre | Formalizar técnicamente el rol ya definido: crear un token/clase semántica para "contenedor" (ej. `--radius-container`) o mapear explícitamente el rol sobre el `rounded-xl` existente — sin cambiar el valor de 12px ni el aspecto visual | `card.tsx`, `dialog.tsx`, `drawer-form.tsx`, `sheet.tsx` | Solo formalización técnica; cero cambio visual |
| D21 | **Descartado** (C3, 2026-08-22) | La premisa histórica ("`TabBadge` quedará sin consumidores tras D08, eliminar") dejó de ser válida. `TabBadge` sigue siendo una abstracción válida para contadores dentro de Tabs, con un consumidor legítimo y permanente: Tareas → Tareas/Peticiones (`src/components/TareasPanel.tsx`, usa `src/components/ui/TabBadge.tsx`), intencional mientras no exista una nueva decisión de producto. El otro consumidor (`StaffConfig.tsx`, sin ruta UI activa verificada) es residual y desaparecerá con D22/C12, pero no condiciona la existencia de `TabBadge` | — | No eliminar `TabBadge.tsx` | `ui/TabBadge.tsx` | Se descarta, no se resuelve: no hay acción pendiente — el componente se queda tal cual |
| D22 | Pendiente | `NegocioConfig.tsx`: código muerto — cero importadores, reemplazado por `ConfigurationPanel`/`MiNegocioPanel`. **Alcance ampliado (C3, 2026-08-22):** arrastra `StaffConfig.tsx` (único importador es `NegocioConfig`, y a su vez deja a `TabBadge` con un consumidor menos si se elimina — ver D21) y `OrganizationSettings.tsx` (único importador también es `NegocioConfig.tsx:31`, mismo patrón) | — | Eliminar los 3 juntos tras re-verificar imports, referencias dinámicas y dependencias ocultas | `components/config/NegocioConfig.tsx`, `components/config/StaffConfig.tsx`, `components/OrganizationSettings.tsx` | Verificación final obligatoria antes de borrar. No se investigó ni se tocó en C3, solo se registra el alcance descubierto |
| D23 | Pendiente | Higiene de componentes: `Badge variant="category"` con API de color vestigial (1 valor posible); `ShowMoreDivider` con default `'miembros más'` específico de Equipo; falso kebab (parece menú, va directo a editar) | Componentes honestos | Limpiar API vestigial; quitar default específico; convertir el falso kebab en botón de editar explícito | `ui/badge.tsx`, `ui/ShowMoreDivider.tsx`, `SucursalTabContent.tsx:281` | — |
| D24 | Pendiente | Se siguen escribiendo variantes `dark:` (ej. botón Activar) pese al light-only congelado | Dark congelado (DESIGN.md Overview) | No escribir `dark:` nuevas; limpiar las existentes al tocar cada archivo por otra razón (no hacer un barrido dedicado) | transversal | — |
| D25 | Pendiente | `LINE_COLORS` triplicado: mismos 8 hex copiados en 3 archivos (es dato de usuario, no token — pero la copia es deuda de código). **Relevancia ampliada** (auditoría 2026-08-22): esta paleta no es solo del picker de Configuración — `lineas.color` es la fuente visual principal del color de cada turno en Agenda (`servicio.linea_color` vía join en `useAgendaData.ts`), así que cualquier inconsistencia acá se ve directamente en Agenda | Una fuente por dato | Consolidar en un único módulo importado por los 3 | `LinesConfig`, `LineQuickEditPopover`, `ServicesConfig`, y por extensión el color que ve el usuario en cada turno de Agenda | — |
| D26 | Pendiente | Estados con color directo sueltos fuera de los clusters D03/D04 | Regla del estado con token | Migrar a tokens | `MpTerminalPaymentDialog`, `MercadoPagoConnect:70`, `TurnoConflictDialog:40`, `ProductoPickerDialog:294`, `ProductoListItem:174`, `NuevoClienteDialog` | — |
| D27 | Pendiente | Tuteo residual (voz canónica: voseo) | Voz rioplatense (PRODUCT.md) | Corregir a voseo | `LineQuickEditPopover:200`, `PinConfigSection:150,173,307` | — |
| D28 | Pendiente | Skeleton de "Compartir tu portal" espeja el layout apilado previo a las pestañas Link/QR (promete un QR que no aparece) | Fidelidad del loading | Ajustar el skeleton al layout con pestañas | `PortalPublicoSection.tsx` | Ya documentado en `docs/MODULOS/turnos-agenda.md` |
| D29 | Pendiente | "Quitar portada" sin texto de progreso (asimetría con "Quitar logo") | Feedback claro | Prop de estado nueva en `PortalCoverUploader` | `PortalCoverUploader.tsx` | Ya documentado en `docs/MODULOS/turnos-agenda.md` |
| D30 | Bloqueado | Indicador de plan Premium sin lugar visible (el dorado `#C39A45` se removió del header en "Variante K" y ya no existe en el código) | — | Decidir si/dónde reaparece el indicador de plan; si vuelve, entra con token, no con hex | — | **Bloqueado** por decisión de producto |
| D31 | **Resuelto** (C2, 2026-08-22) | `maxLength` de redes sociales inconsistente: 80 (alta) vs 120 (detalle) vs 80 (importador, no capturado en el hallazgo original) | Escala de `maxLength` (DESIGN.md Forms) | Instagram/TikTok/Otra red social a 120 en los 3 puntos: `NuevoClienteDialog.tsx` (Zod + HTML), `ClienteDetailDialog.tsx` (ya estaba en 120, verificado sin tocar), `parseImportFile.ts` (`normalizeText`). Sin residuos de 80 para estos 3 campos | `clientes/NuevoClienteDialog.tsx`, `clientes/import/lib/parseImportFile.ts` | Cero migración de DB (columna `text` sin restricción). `nombre`/`apellido` del importador quedan en 80 a propósito — son campos distintos, no forman parte de D31 |
| D34 | Pendiente | Migración progresiva de `input type="date"`/`type="time"` nativos al canon `DatePicker`/`TimePicker` (DESIGN.md § Fecha y hora). Relevados antes de Build A: ~19 `type="date"` en 10 archivos y ~12 `type="time"` en 7 — C2 solo migró `BloqueosSection` y `ScheduleEditor` (Build A) y dejó a propósito el resto sin tocar | Fecha y hora usan `DatePicker`/`TimePicker` — el picker nativo no es patrón válido para código nuevo | Migrar por clusters, en pasadas separadas de este build: Agenda (`NewAppointmentDialog`, `DayOffDialog`, `UnavailableSlotDialog`, `AppointmentDetailDialog`), Equipo (`EquipoSucursalPanel`, `BarberSucursalesGeneralSection`), Finanzas (`GastosPanel`, `DeudasPanel`, `InversionesPanel`), Tareas (`TareaFormDialog`, `TareaRecurrenteFormDialog`). **El Portal público (`reservar/*`, ej. `DatosClienteStep.tsx`) queda separado explícitamente** — es una superficie distinta (white-label, cliente final) y no migra junto con Operate; se revisa cuando corresponda trabajar esa superficie | Los 9 archivos listados arriba + `reservar/DatosClienteStep.tsx` (aparte) | Cada cluster es su propio build acotado — no migrar todo de una vez. Verificar `min`/restricciones cruzadas existentes (ej. `EquipoSucursalPanel` usa `min={fechaInicioWatch}`) al mapear a `fromDate`/`toDate` del nuevo `DatePicker` |
| D33 | Pendiente | Ignore de `layout-transition` sobre `AppSidebar.tsx` en `.impeccable/config.json` (persistido durante C1, cubre la animación de `width`/`padding`/`margin` de la coreografía de colapso del riel) — es una excepción **temporal**, no una exención permanente del sistema | A definir en C11 — Motion | **C11 debe revalidar explícitamente este ignore** contra el sistema de motion que se defina ahí: mantenerlo, migrar la animación a `transform`, o reformular la regla. No se resuelve ni se retira acá | `.impeccable/config.json` (detector), `AppSidebar.tsx` | No dejar que esta excepción sobreviva a C11 sin revisión |

## Limpieza documental (fase posterior, con validación explícita)

Clasificación hecha durante la exploración. **Nada se mueve ni se elimina todavía.**

| Documento | Clasificación | Antes de tocar |
|---|---|---|
| `docs/archivo/CRITERIOS_DISEÑO.md` | **Archivado** (C2, 2026-08-22) | Las 5 reglas vigentes que quedaban sin migrar (obligatorio/opcional, escala de `maxLength`, errores inline vs. toast, `EmptySelectHint`, `QuickApplyCard`) ya están en `DESIGN.md` § Forms. Verificación final: sin reglas activas adicionales sin representar |
| `docs/archivo/AUDITORIA_VISUAL_MI_NEGOCIO.md` | **Archivado** (2026-08-22) | Auditoría de jun 2026, previa a la migración de Mi Negocio/Configuración al canon `DrawerForm`+RHF/Zod (cerrada jul 2026). Sus 8 "dudas explícitas" quedaron resueltas por esa migración; nada material sin capturar |
| `AUDITORIA_DATOS_ESTADISTICAS.md` | **Sin archivar, pendiente de revisión de datos** | No es documentación de diseño (inventario de schema Supabase para Estadísticas). Es la única fuente escrita del modelo dual `venta`/`ingresos`; no hay `docs/MODULOS/estadisticas.md` que la reemplace. Si sigue vigente tras el build de reestructuración de Estadísticas es una pregunta de datos, no de diseño — no se resuelve en esta pasada |
| `docs/ESTADO_ACTUAL.md`, `docs/DECISIONES.md`, `docs/MODULOS/*`, `docs/INDICE.md` | Vigentes | En `DECISIONES.md`, la regla de chips quedó duplicada con DESIGN.md — dejar ahí solo el "porqué" en la pasada de limpieza |
